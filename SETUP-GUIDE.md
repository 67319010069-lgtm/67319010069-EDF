# 🚀 Eduflow Clone - Setup Guide

## สิ่งที่ต้องทำต่อ (Next Steps)

### 1. ตั้งค่า Supabase (สำคัญมาก!)

คุณต้องทำขั้นตอนนี้ก่อนรันโปรเจกต์:

#### ขั้นตอนที่ 1: สร้าง Supabase Project
1. ไปที่ [https://supabase.com](https://supabase.com)
2. สมัครสมาชิก/เข้าสู่ระบบ
3. คลิก "New Project"
4. ตั้งชื่อโปรเจกต์และรหัสผ่านฐานข้อมูล
5. เลือก Region ที่ใกล้ที่สุด (แนะนำ Southeast Asia)
6. รอให้โปรเจกต์สร้างเสร็จ (ประมาณ 2 นาที)

#### ขั้นตอนที่ 2: รัน SQL Script
1. ในหน้า Supabase Dashboard ไปที่ **SQL Editor** (เมนูซ้าย)
2. คลิก **New Query**
3. คัดลอกเนื้อหาทั้งหมดจากไฟล์ `supabase-setup.sql`
4. วางในช่อง SQL Editor
5. คลิก **Run** (หรือกด Ctrl+Enter)
6. ตรวจสอบว่าไม่มี error

#### ขั้นตอนที่ 3: สร้าง Storage Buckets
1. ไปที่ **Storage** (เมนูซ้าย)
2. คลิก **New Bucket** สร้าง 3 buckets:
   - ชื่อ: `avatars` → เลือก **Public bucket** ✓
   - ชื่อ: `thumbnails` → เลือก **Public bucket** ✓
   - ชื่อ: `course-files` → เลือก **Public bucket** ✓

#### ขั้นตอนที่ 4: คัดลอก API Keys
1. ไปที่ **Settings** > **API** (เมนูซ้าย)
2. คัดลอก:
   - **Project URL** (ตัวอย่าง: `https://xxxxx.supabase.co`)
   - **anon public** key (ตัวอย่าง: `eyJhbGc...`)

#### ขั้นตอนที่ 5: ตั้งค่า Environment Variables
1. เปิดไฟล์ `.env.local` ในโปรเจกต์
2. แก้ไขเป็น:
```env
NEXT_PUBLIC_SUPABASE_URL=ใส่_Project_URL_ของคุณ
NEXT_PUBLIC_SUPABASE_ANON_KEY=ใส่_anon_key_ของคุณ
```

### 2. รันโปรเจกต์

```bash
npm run dev
```

เปิดเบราว์เซอร์ที่ [http://localhost:3000](http://localhost:3000)

---

## 📝 คำสั่งที่ใช้บ่อย

```bash
# ติดตั้ง dependencies
npm install

# รันโปรเจกต์ (development)
npm run dev

# Build โปรเจกต์ (production)
npm run build

# รันโปรเจกต์ (production)
npm start
```

---

## ✅ ทดสอบระบบ

### ทดสอบแบบผู้สอน (Instructor)
1. ไปที่หน้าแรก → คลิก **สมัครสมาชิก**
2. กรอกข้อมูล เลือกประเภท **ผู้สอน**
3. เข้าสู่ระบบ → ไปที่ **แดชบอร์ด**
4. คลิก **สร้างคอร์สใหม่**
5. กรอกข้อมูลคอร์ส เพิ่มบทเรียน
6. เลือก **เผยแพร่คอร์สทันที** ✓
7. คลิก **สร้างคอร์ส**

### ทดสอบแบบผู้เรียน (Student)
1. ออกจากระบบ
2. สมัครสมาชิกใหม่ เลือกประเภท **ผู้เรียน**
3. ไปที่ **คอร์สเรียน**
4. เลือกคอร์สที่สร้างไว้
5. คลิก **ลงทะเบียนเรียน**
6. คลิก **เข้าเรียน**
7. ดูบทเรียน → คลิก **ทำเสร็จแล้ว**
8. เมื่อเรียนจบทุกบท จะมีฟอร์มรีวิว

---

## 🎯 ฟีเจอร์ที่สร้างเสร็จแล้ว

### ✅ Authentication
- สมัครสมาชิก (อีเมล, รหัสผ่าน, ชื่อ-นามสกุล)
- เข้าสู่ระบบ
- ออกจากระบบ
- เลือกบทบาท (ผู้เรียน/ผู้สอน)

### ✅ ผู้สอน (Instructor)
- แดชบอร์ดแสดงคอร์สทั้งหมด
- สร้างคอร์สใหม่
- เพิ่มบทเรียน (วิดีโอ, ข้อความ, PDF, เสียง)
- เลือกสถานะ Draft/Published
- แสดงจำนวนบทเรียนในแต่ละคอร์ส

### ✅ ผู้เรียน (Student)
- เรียกดูคอร์สทั้งหมด
- ค้นหาคอร์ส
- กรองตามหมวดหมู่
- ดูรายละเอียดคอร์ส
- ลงทะเบียนเรียน
- เรียนผ่านหน้า Learning Interface
- ติดตามความคืบหน้า (Progress Bar)
- ทำเครื่องหมายบทเรียนที่เรียนจบ
- ให้คะแนนและรีวิวคอร์ส

### ✅ UI/UX
- ดีไซน์สวยงาม Premium
- Glassmorphism effects
- Gradient backgrounds
- Smooth animations
- Responsive (รองรับมือถือ)

---

## 🗂 โครงสร้างโปรเจกต์

```
67319010069/
├── src/
│   ├── app/
│   │   ├── auth/
│   │   │   ├── login/page.tsx          # หน้าเข้าสู่ระบบ
│   │   │   └── signup/page.tsx         # หน้าสมัครสมาชิก
│   │   ├── courses/
│   │   │   ├── [id]/page.tsx           # หน้ารายละเอียดคอร์ส
│   │   │   └── page.tsx                # หน้าคอร์สทั้งหมด
│   │   ├── dashboard/page.tsx          # แดชบอร์ด
│   │   ├── instructor/
│   │   │   └── create-course/page.tsx  # สร้างคอร์ส
│   │   ├── learn/
│   │   │   └── [id]/page.tsx           # หน้าเรียน
│   │   ├── globals.css                 # CSS หลัก
│   │   ├── layout.tsx                  # Layout หลัก
│   │   └── page.tsx                    # หน้าแรก
│   └── lib/
│       └── supabase.ts                 # Supabase client
├── supabase-setup.sql                  # SQL สำหรับสร้างฐานข้อมูล
├── .env.local                          # ตัวแปร environment
├── package.json
├── tailwind.config.js
└── README.md
```

---

## 🎨 เทคโนโลยีที่ใช้

- **Frontend**: Next.js 14, React 18, TypeScript
- **Styling**: Tailwind CSS 3
- **Backend**: Supabase (PostgreSQL + Auth + Storage)
- **Icons**: React Icons

---

## 📊 ฐานข้อมูล

### ตาราง (Tables)
1. **profiles** - ข้อมูลผู้ใช้
2. **courses** - ข้อมูลคอร์ส
3. **lessons** - บทเรียน
4. **enrollments** - การลงทะเบียนและความคืบหน้า
5. **reviews** - รีวิวและคะแนน

---

## 🔧 ฟีเจอร์ที่ยังไม่ได้ทำ (Optional)

- [ ] แก้ไขคอร์ส (Edit Course)
- [ ] ลบคอร์ส (Delete Course)
- [ ] แก้ไขโปรไฟล์ (Edit Profile)
- [ ] อัปโหลดรูปโปรไฟล์
- [ ] Analytics สำหรับผู้สอน
- [ ] Recommended Courses
- [ ] ระบบค้นหาขั้นสูง

---

## ❓ แก้ปัญหา

### ไม่สามารถเข้าสู่ระบบได้
- ตรวจสอบว่า SQL script รันเสร็จแล้ว
- ตรวจสอบ `.env.local` ว่าใส่ค่าถูกต้อง
- Restart dev server (`Ctrl+C` แล้ว `npm run dev` ใหม่)

### ไม่เห็นคอร์สที่สร้าง
- ตรวจสอบว่าเลือก **เผยแพร่คอร์สทันที** ตอนสร้าง
- ตรวจสอบ RLS policies ใน Supabase

### รูปภาพไม่แสดง
- ตรวจสอบว่าสร้าง Storage buckets แล้ว
- ตรวจสอบว่า buckets เป็น **Public**
- ใช้ URL รูปภาพจาก internet (เช่น Unsplash)

---

## 📞 ติดต่อ

หากมีปัญหาหรือคำถาม สามารถดูเอกสารได้ที่:
- [Supabase Docs](https://supabase.com/docs)
- [Next.js Docs](https://nextjs.org/docs)
- [Tailwind CSS Docs](https://tailwindcss.com/docs)

---

**สร้างโดย**: Eduflow Team  
**เวอร์ชัน**: 1.0.0  
**อัปเดตล่าสุด**: 2026-02-11
